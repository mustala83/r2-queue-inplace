merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: default
    batch_size: 1
    queue_conditions:
      - base=main
      - check-success=r2-ci

pull_request_rules:
  - name: queue it
    conditions:
      - base=main
    actions:
      queue:
        name: default
